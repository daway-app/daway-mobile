/// A device-level setting the app can send the user to (an app can read its
/// permission state, but only the user can change it, in system settings).
enum DeviceSetting { notifications, location }
